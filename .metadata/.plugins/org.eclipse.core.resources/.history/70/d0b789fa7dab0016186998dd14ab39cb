package gui;

/**
 * Class for organizing the graphical user interface.
 *
 */
public class GUI {
    
    
    /**
     * Start the GUI and show it to the user.
     */
    public static void bringUpGUI() {
	
    }
    
    
    /**
     * For the console prototype:
     * This method should be called if the user has to answer a question (e.g. specifying
     * the folder path). Returns the answer after it is given (This must be checked!)
     */
    public String askFor(String question) {
	return null;
	
    }
    
    
    
    // handle inputs via listeners here...
    
    
    
}
