package gui;

/**
 * Class for organizing the graphical user interface.
 *
 */
public class GUI {

	/**
	 * Start the GUI and show it to the user.
	 */
	public static void bringUpGUI() {

	}


	/**
	 * This method is called if the user has to answer a question (e.g. specifying the folder
	 * path). Returns the answer after it is correctly given (which is checked).
	 * 
	 * @param question The question to the user
	 * 
	 * @return The answer, represented as a String
	 */
	public String askFor(String question) {
		return null;

	}

	// handle inputs via listeners here...

}
